# Microsoft Outlook MCP Server - Environment Variables
# Copy this file to .env and fill in your values

# REQUIRED - Application (client) ID from Azure Portal Overview
# Get this from: Azure Portal → App registrations → Your app → Overview → Application (client) ID
OUTLOOK_CLIENT_ID=your-application-client-id-here

# REQUIRED - Client secret Value (not Secret ID) from Certificates & secrets
# Get this from: Azure Portal → App registrations → Your app → Certificates & secrets → New client secret
# ⚠️ IMPORTANT: Copy the "Value" column, NOT the "Secret ID". The Value is only shown once!
OUTLOOK_CLIENT_SECRET=your-client-secret-value-here

# OPTIONAL - Redirect URI (default is already set)
# Only change if you have a specific redirect URI configured in Azure
OUTLOOK_REDIRECT_URI=https://login.microsoftonline.com/common/oauth2/nativeclient

# OPTIONAL - Token cache file path
# Default: token.json (in project root)
# Set a custom path if you want to store tokens elsewhere
OUTLOOK_TOKEN_PATH=token.json

# OPTIONAL - Workspace path for file operations
# Set this to restrict file I/O to a specific directory
# Example: WORKSPACE_PATH=./workspace
# WORKSPACE_PATH=

# OPTIONAL - Backend API settings (for token provider)
# Only needed if using a backend token provider
# BACKEND_API_URL=
# BACKEND_API_KEY=
# MCP_IDENTIFIER=outlook-mcp
# AGENT_ID=

